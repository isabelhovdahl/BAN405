# images

Figures and illustrations used in the lecture notebooks and markdown files, in a single flat folder.

Referenced from a notebook in a week folder as:

```markdown
![Control structure](../images/control_structure.png)
```

Images are included in `ban405-workspace.zip` alongside `data/`, so that a downloaded notebook
renders correctly.

*Images will be added here as the material is developed.*
